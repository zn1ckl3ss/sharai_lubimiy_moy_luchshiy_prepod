global:
  scrape_interval: 15s # Как часто собирать метрики

scrape_configs:
  - job_name: 'fastapi-app'
    scrape_interval: 5s
    static_configs:
      - targets: ['web-app:8000'] # Имя сервиса из docker-compose